>Download the front page HTML file of www.meetangee.com
CHECK
>scan it as fast as you can
просканил (не знаю; наверняка можно это сделать быстрее)
>download every file the page references
CHECK
>These files must be downloaded as fast as possible, so download them in parallel
CHECK
>Make hash of every file with Adler 32 algorithm
CHECK
>Print list of file names and their hashes to the command line
класс для хранения набросал; опять же, гляну best practices по секурности и ограничению привелегий на изменение
>Highlight the biggest and smallest (in size) file
тут непонятно, пока думал сделать отдельно выхлоп наибольшего и наименьшего файлов, ага
>documentation (must be in English)
тут, так понимаю, набросать в комментариях в каждом исходнике, что делает каждая функция? если да, то тоже сделаю
>The program must be developed in C++ (using OOP style)
завтра сделаю рефактор, да
>and must run on Ubuntu 16.04
на ней и работаю
>He can also use any library in Ubuntu 16.04 repository if it helps
CHECK
>For compilation he can use any command line tool (i.e. no Eclipse project...) or command
пока компилирую через g++, набросаю небольшой makefile
>He is not allowed to use any external programs during execution of his application
CHECK
>Write the interface of his code so it could be reused in some other program.
эм тут тоже не понял, на самом деле (т.е. если это так, чтобы функции, которые логически принадлежат к, скажем, одной библиотеке, находились в отдельном файле + был header для возможности компиляции файла как библиотеки, то так и планировал пока сделать)
 
там ещё, на самом деле, есть нюансы насчёт файлов (e.g. некоторые файлы в html встречаются больше одного раза, я сделал уникальную выборку в итоге, плюс есть вероятность, что какие-либо из файлов окажутся одного размера, но конкретно в данном случае такого нет, поэтому я не обрабатывал такой случай); было бы ещё классно проверить всё на чистой системе, чтобы убедиться, какие именно пакеты нужно ставить, чтобы скомпилировать программу (у меня на машине подцепилось вообще всё с ходу, на чистой системе так не будет); что с этим делать пока не знаю!
